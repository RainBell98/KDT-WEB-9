import React from 'react';
import ReactDOM from 'react-dom/client';
import App from './App';
// import Prac1Component from './Prac1Component';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>
);
