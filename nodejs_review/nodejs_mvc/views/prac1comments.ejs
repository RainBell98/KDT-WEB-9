<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script src="https://cdn.jsdelivr.net/npm/axios/dist/axios.min.js"></script>
</head>
<body>
    <h1>댓글 전체 목록</h1>
    <a href="/">홈으로 이동</a>
    <!-- <form name="addUser"> -->
        <fieldset>
            <legend>회원추가</legend>
            이름: <input type="text" name="name" id="name"><br>
            성별: <input type="text" name="gender" id="gender" placeholder="w,m으로 입력"><br>
            전공: <input type="text" name="major" id="major">
            <button type="button" onclick="axiosPost()">회원추가</button>
        </fieldset>
    <!-- </form> -->
    
    <ul>
        <% for(let i = 0; i < lists.length; i++){ %>
            <li>
                <b><%= lists[i].id %></b>
                <b><%= lists[i].name%></b>
                <b><%= lists[i].gender%></b>
                <b><%= lists[i].major%></b>
                <!-- <b><%= lists[i].date%></b> -->
            </li>
        <%}%>
    </ul>
    <script>
        let uid = 5
        const userid = document.getElementById("name")
        const usergender = document.getElementById("gender")
        const major = document.getElementById("major")
        const form = document.forms['addUser']
        async function axiosPost(){
            uid += 1
            const data = {
                // id: uid,
                name: userid.value,
                gender: usergender.value,
                major:major.value
            }
            console.log(data)
            const res = await axios({
                method: "POST",
                url:"/add",
                data ,
            })
            window.location.reload()
        }

        
    </script>
</body>
</html>